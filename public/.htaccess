<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /newsite/

  # If the request is not a file or directory, serve index.html (SPA routing)
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
